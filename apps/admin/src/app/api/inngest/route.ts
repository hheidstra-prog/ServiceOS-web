import { NextResponse } from "next/server";

export async function PUT() {
  // Inngest webhook handler - stub for now
  return NextResponse.json({ success: true });
}

export async function POST() {
  // Inngest webhook handler - stub for now
  return NextResponse.json({ success: true });
}
